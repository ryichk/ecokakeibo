
<div id="kakeibo">
  <div id="input_consmonth">
    <table class="input_consmonth1">
      <div class="contents">
        <ul id="result">
        </ul>
        <tr>
          <th>
            <%= image_tag 'yasai.png', :size => "100x100" %> 野菜のおかず
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, options_for_select(@yasai), {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'you.png', :size => "100x100" %> 肉のおかず
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @niku, {}, {:include_blank => true} %>
             <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'wa.png', :size => "100x100" %> 魚介のおかず
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @sakana, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'tamago.png', :size => "100x100" %> 卵・大豆製品
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @tamago, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'siru.png', :size => "100x100" %> 汁物
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @soup, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'gohan.png', :size => "100x100" %> ごはんもの
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @gohan, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'itary.png', :size => "100x100" %> パスタ・グラタン
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @pasta, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'men.png', :size => "100x100" %> 麺類
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @men, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'pan.png', :size => "100x100" %> パン・ピザ
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @pan, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'kona.png', :size => "100x100" %> 粉もの
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @kona, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'ten.png', :size => "100x100" %> 揚げ物
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @agemono, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'nabe.png', :size => "100x100" %> 鍋もの
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, options_for_select(@nabe), {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
        <tr>
          <th>
            <%= image_tag 'kobati.png', :size => "100x100" %> 小鉢
          </th>
          <td>
            <%= form_for @meal do |f| %>
              <%= f.hidden_field :user_id, value: current_user.id %>
              <%= f.select :cuisine_id, @kobachi, {}, {:include_blank => true} %>
              <%= f.submit "登録", class: "btn-syoku" %>
            <% end %>
          </td>
        </tr>
      </div>
    </table>
  </div>
</div>
